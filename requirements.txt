# Core: the engine itself is pure Python and needs none of these.
# These are for loading data, plotting results and checking gradients.
scikit-learn>=1.3    # datasets, stratified splits, baseline classifiers
matplotlib>=3.7      # training-curve plots
numpy>=1.24          # pulled in by the above

# Optional
torch>=2.0           # tests/test_engine.py checks gradients against autograd
graphviz>=0.20       # dlscratch.viz.draw_dot (also needs the graphviz binaries)
pytest>=7.0          # test runner (tests also run via `python tests/test_engine.py`)
